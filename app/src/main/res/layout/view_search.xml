<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="searchViewModel"
            type="ua.in.khol.oleh.touristweathercomparer.viewmodel.SearchViewModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimaryDark"
            android:gravity="center"
            android:padding="4dp"
            android:text="@string/city"
            android:textColor="@color/text_alert"
            android:textSize="@dimen/title_size" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/category_padding"
            android:text="@string/enter_name"
            android:textColor="@color/colorAccent"
            android:textSize="@dimen/text_size" />

        <EditText
            android:id="@+id/inputEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/default_margin"
            android:padding="8dp"
            android:textColor="@color/colorPrimary"
            android:textSize="@dimen/text_size" />

        <ListView
            android:id="@+id/namesListView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/nav_item_margin"
            android:paddingVertical="@dimen/default_margin_half" />
    </LinearLayout>
</layout>
